# Phase 3 — Finalization Handoff & Parallel Work Streams

Owner: fromage3900 · Last verified live in Studio: 2026-07-24
Entry point for any agent picking up work. Pairs with [PLAYTEST_NOTES.md](PLAYTEST_NOTES.md) (live issue tracker) and [PHASE3_FINALIZATION_PLAN.md](PHASE3_FINALIZATION_PLAN.md).

> **⭐ Read [PHASE3_CORE_SCAFFOLDING.md](PHASE3_CORE_SCAFFOLDING.md) first** — it is the authoritative, live-verified (2026-07-24) map of the core loop and close-out state. The "Done & verified live" / "publishing criteria" sections lower in *this* file are older and partly stale (they describe the pre-replacement companion mesh, etc.). Where they disagree, the core-scaffolding doc wins.

## Session close-out (2026-07-24, later — companion mesh fixed, Zundarooms geometry live)
- **Companion mesh finally resolved.** User imported `zundapalupdate4` as a proper skinned-mesh avatar (via Studio's Avatar Importer — confirmed live: single skinned `MeshPart` + `AnimationController`, no `Humanoid`, same shape as the parrot rig). It was sitting loose in `Workspace` after import; moved into `ServerStorage.CompanionVisualCatalog.Prefabs.zundamon` (and `.zundapal` as a transitional-fallback copy), replacing the old broken empty-`MeshId` placeholder. Verified live: companion spawns, animates via the `Animator`, upright, human-sized. **Remaining, separate gap: renders fully white/untextured** (no material/`SurfaceAppearance` applied) — this is the pre-existing Stream C2 item, not a new regression.
- **Asset pipeline proven end-to-end for real** (`503f437`): ran `tools/asset-pipeline/upload-batch.js` for real with the user's own Open Cloud API key. Roblox's Assets API rejects `.obj` for Model uploads ("not supported yet") — added `convert_obj_to_glb.py` (headless Blender `--background --python`) to convert first. 9/9 uploads succeeded under the confirmed-correct creator (`game.CreatorId=3930496852`). Assembled 8 of them into `ServerStorage.AssetLibrary.Zundarooms.RoomSegment` — verified live, all 6 corridor segments clone the authored prefab correctly, user confirmed visually in Play mode.
- **Reconfirmed a real footgun**: `ServerStorage.AssetLibrary`/`CompanionVisualCatalog` are place-baked (non-Rojo) instances. A `RoomSegment` build was lost once mid-session on an Edit→Play transition before an explicit save — rebuilt and it held on the second attempt, but the takeaway stands: **File → Save/Publish after any manual ServerStorage instance change**, or a Studio restart (not just a Play cycle) can revert it to the last saved state. See [[rbxl-baked-instances]] in memory.
- **Collaboration model for `AssetLibrary`**: since none of this is Rojo-tracked, git/Rojo cannot keep two collaborators' local Studio sessions in sync for these instances. Either (a) enable Roblox **Team Create** on this place for real-time shared editing (recommended if both are actively adding meshes), or (b) strict manual discipline — publish immediately after every addition, and each collaborator syncs to latest before adding their own, since baked-instance changes don't merge (last publish simply wins and can silently clobber the other person's unpublished local additions).
- **`ResourceVisualAuthoringPlugin` is now fully usable by anyone with Studio access** to make any mesh harvestable: import the mesh into Studio (Toolbox/Avatar Importer/manual placement — never raw asset-ID entry), place the resulting Model at `ServerStorage.AssetLibrary.ResourceNodes.<variant name>` (name must match what's typed into the plugin's "Variant name" field), tag the target part `ResourceNode`, click Apply. The plugin/`ResourceVisualService` now checks that path first, before ever touching `InsertService`/`ContentProvider` — no permission errors possible for content sourced this way.

## Session close-out (2026-07-24, deep session — companion/asset/FX/NPC/Zundarooms, `12fefce`→`45b9c2d`)
Long continuous session, ~15 commits. Full picture, newest first:

- **Companion identity fully consolidated**: `zundapal`/`zundamon` merged to one canonical id `"zundamon"` across `CompanionConfig`, `CompanionVisualConfig`, `VNDialogueData` (merged the two parallel dialogue pools into one voice), with a save-data migration (`active_companion`/`companion_owned_*`/`companions_set.*`) for old saves. Fixed a hardcoded `"zundapal"` speaker bug in `VNController`'s `buildCompanionTree`. Rewrote the welcome VN popup + tutorial companion step so the narrator and the companion read as one character, not two.
- **Companion mechanics**: `CompanionManager.server.lua` now detects `Humanoid`/`AnimationController` on a cloned prefab and gets-or-creates an `Animator`; added an idle/walk animation-state driver to the existing per-frame follow loop (nil-safe — does nothing until real animation ids exist in `CompanionVisualConfig`). The user's parrot rig (`Workspace.animal-parrot`, confirmed real `AnimationController` + jointed limbs, no `Humanoid`) is placed at `ServerStorage.CompanionVisualCatalog.Prefabs.parrot` for live verification.
- **⚠️ Companion mesh still broken (pre-existing, not this session's fault)**: `Prefabs.zundapal`/`zundamon` is confirmed empty (both MeshParts have `MeshId = ""`) and the level mesh is gone from `Workspace`. **Lead**: `G:\ZundamonsKitchenWORKINGBASELINE.rbxl` (dated 2026-07-21) may have the mesh in a working state — check there before re-importing `zundapalupdate4.fbx` from scratch. That FBX itself is confirmed a real rig (59 bones, 36 skin clusters, 2 animation takes) — import via Studio's **Avatar Importer** specifically (same path that worked for the parrot), not a generic mesh importer.
- **Asset pipeline overhaul**: established `ServerStorage.AssetLibrary` as the canonical prefab-first convention (mirrors the pre-existing, working `CompanionVisualCatalog.Prefabs` pattern) — `ResourceVisualService.lua` now checks `AssetLibrary.ResourceNodes.<variant>` before ever touching `InsertService`/`ContentProvider`. `ResourceVisualAuthoringPlugin.server.lua` (the Studio authoring tool) fixed for its three real bugs: forced `root.Transparency=1` before confirming a replacement rendered (invisible-in-Play bug), never set `TextureId` on its bare-mesh fallback (untextured/white bug), never resized collision to the visual's actual extents (mismatch bug) — plus a `parseTransform()` multi-return truncation bug that broke the Y-offset. `Zundarooms.RoomSegment` uses the same convention now (see below) but is not yet populated.
- **Zundarooms expanded**: was a single ~110-stud room, now a chained corridor of `segmentCount` (6) segments with sparse flickering light fixtures (deliberate unevenness for the liminal-space read) and an authored-prefab override slot (`AssetLibrary.Zundarooms.RoomSegment`) that overlays custom geometry on top of the always-built procedural collision box. Verified live: 6 floors/12 walls/30 fixtures, exit position and entity chase both correct. **Candidate assets found for the prefab**: `G:\EnvironmentPortfolio\_AssetLibrary\Greybox_Kit\*.obj` (modular wall/column/floor/beam/step blocks) — needs the user to import via Studio Toolbox (I cannot upload files to Roblox myself; no scripted upload API exists).
- **NPC systems**: patrol NPCs (`Traveler_01-03`, `Merchant_01`) previously had zero interaction surface — `QuestManager`'s `npc_chats` "talk to NPC" tracking could never fire. Added a `ClickDetector` per NPC that records the same data server-side directly (not via a misdirected `RemoteEvent:FireServer()` call from the server — that direction doesn't work). `Merchant_01`'s click now opens the existing gold-based Furniture Shop (`FurniturePlacement.client.lua`, real keybind is **H**, not the stale "press P" the code used to claim — fixed) via a new `OpenMerchantShop` RemoteEvent.
- **Starter gift now actually grants gold**: claim button used to just close the panel. Added `ClaimStarterGift` RemoteEvent + `StarterGiftServer.server.lua` granting 500 gold via `RewardCore.settle`, gated by a persisted `starter_gift_claimed` flag (one-time per player).
- **Killed the "squares in the sky" bug** — three separate broken-asset issues layered together: `AmbientParticles.lua`'s 4 particle texture ids all fail (`AssetFetchStatus.Failure`) → disabled the module entirely pending real assets; `SkyOverlay.client.lua`'s Tile-mode crystal/starry `ImageLabel` layers rendered as a grid of pale diamonds once an unrelated crash (below) stopped masking them → removed, kept only the Fit-scaled glow layer; `DayNightSky.server.lua`'s daytime skybox intentionally used the identical faceted nebula texture on all 4 side cube-faces → mixed with the softer purple-nebula texture per face + bumped `SkyConfig.atmosphere` haze/glare (user's explicit call: keep the nebula look, just tone it down).
- **`GuiService:GetEngineFeature` doesn't exist as a real API** — 8 files (`SkyOverlay`, `PostProcessing`, `AmbientParticles`, `CelOutline`, `ChromaticAberration`, `GroundMist`, `MagicCircle`, `WhimsicalOverlay`) called it and crashed on load every time; only `PeaWheelController.lua`'s `GuiService.ReducedMotionEnabled` direct-property-read was ever correct. Fixed everywhere — this alone had been silently breaking FXController's whole module load.
- **Fixed a live, non-Rojo baked Script crash**: `japanese shrine lantern`'s `CoreSystem` called `require()` on a numeric attribute value instead of the `ModuleScript` instance — Roblox reads `require(number)` as "load by asset id", hitting the same account-ownership permission wall as everywhere else. Corrected to require the instance directly; scanned the whole workspace, only this one instance had the pattern.
- **Asset inventory**: 207 unique `rbxassetid://` references across 30 files (`UIAssets.lua` 49, `HarvestNodeVariants.lua` 26, `SoundServiceSetup.server.lua` 26, `NPCConfig.lua` 24, `ResourceVisualCatalog.lua` 24, `VNDialoguePortraits.lua` 22, `DayNightSky.server.lua` 19 are the top concentrations). **`ContentProvider:PreloadAsync` is unreliable when called from an injected/Assistant script context** — it reports Failure even for assets confirmed visually working, so automated broad verification isn't trustworthy here. **Recommend using Studio's own Asset Manager panel** (flags broken/unavailable assets with a warning icon) to triage the rest, rather than more scripted probing.
- **Drive scan for reusable assets** (`G:\EnvironmentPortfolio`, `MelodiaMelusina*`): found `Greybox_Kit` (Zundarooms candidate, above), `sakurapetal_LOD.glb`/`sakurapetal_particle.glb` (could replace the broken sakura particle texture), and `ZUNDAMON_DESIGN_BIBLE.md` confirming full legal clarity (Zundamon/Zunko project explicitly permits derivative works — no licensing blocker on any of this).
- **A `/loop 15m` cron job is active this session** (job `daee5dea`, session-only, dies if this Claude session closes, 7-day hard cap regardless) — continues checking for the `RoomSegment` prefab, auditing broken assets found via direct evidence, and verifying+committing. If you're reading this in a later, different session, that loop is no longer running — treat every item above as the real state, not "in progress."

## Session close-out (2026-07-23, night — PeaWheel/HUD dispatch fixes, `12fefce`)
- **Root-caused why the Pea Wheel felt unclickable:** `RunService:IsEdit()` throws `lacking capability Plugin` when called from a normal client LocalScript in this Roblox version — every hub-button click was silently crashing before `toggle()` could run. Removed the dead edit-mode guard entirely (`PeaWheelController.lua`).
- Removed two rogue `UserInputService.InputBegan` listeners that bypassed `UIActionRegistry` (Tab/Q for the wheel, K for Outfit Wardrobe) — K was opening both Crafting and Wardrobe at once. Both now have proper registry actions (`peawheel`, `wardrobe`).
- Fixed the real cause of "dead" HUD buttons (materials/quests/cook/inventory/compendium): `UI_ActionRegistry.dispatch()` used to warn-and-drop a click if the panel's callback hadn't registered yet (a load-order race `PeaWheelBootstrap` only ever diagnosed, never fixed). Now queues and auto-fires once `registerCallback()` lands.
- `CozyModalShell.openShell()` was stripping the pixel-offset half of `Position` every time a panel opened, pushing CompanionShop/Materials off-screen — deleted the offending line.
- Wired `ZundaSoundController` (never `require()`'d anywhere, so `_G.ZundaSoundController` was always nil) + added the missing `ButtonClick` call; widened `AmbientZoneAudio`'s rolloff (zones sat beyond audible range from spawn).
- Found + fixed a `UIHelper.getCategory` colon/dot method mismatch that crashed the Pouch panel the moment the dispatch fix made it reachable.
- **Next up (not yet started this session):** companion identity consolidation (Zundamon/Zundapal → one name, user-confirmed), asset pipeline overhaul (`ServerStorage.AssetLibrary` convention to kill `InsertService` permission failures) + `ResourceVisualAuthoringPlugin` invisible/untextured/collision bugs, companion idle/walk animation verified against the user's already-imported parrot rig before Zundamon's own rig import. Full plan: see `C:\Users\froma\.claude\plans\ok-review-project-status-partitioned-turing.md` on the user's machine (session-local, not in repo).
- **Known regression, not caused by this session's edits:** the companion (`zundapal`) is currently failing to spawn entirely — `CompanionManager` can't resolve `Workspace.Meshes/zundapalupdate4` or a `ServerStorage.CompanionVisualCatalog.Prefabs.zundapal` anymore. Likely moved/removed during the user's own parrot-rig import testing. Needs the level mesh restored or a prefab placed before companions work again.
- **Also unrelated to this session:** `GuiService.GetEngineFeature` errors in `SkyOverlay`/`PostProcessing`, breaking `FXController`'s module load — a separate, pre-existing issue surfaced by an engine/API change.

## Session close-out (2026-07-23, evening — Stream A + UI layering DONE)
- **Pushed through `48bca32` on `main`.** Stream A is fully fixed and verified live (`41ca95e`), the DisplayOrder ladder landed (`27f32dd`, see [FX_UI_LAYERING_PLAN.md](FX_UI_LAYERING_PLAN.md)), and 8 dead-generation GUI shells were destroyed + the starter-gift popup gated behind the tutorial (`ece9cec`).
- **⚠️ To ship the in-place fixes: File → Publish from Studio once.** The stray `ZundaResourceVisualAuthoring` plugin Model lives in the `.rbxl` (not Rojo), so its deletion — and everything else — only sticks after a publish/save.
- **⚠️ Starter gift rewards are display-only** — claim just closes the panel; no server grant exists (FX_UI_LAYERING_PLAN.md item 10). Wire or label before monetization review.
- **Known maintenance smell:** `site/` (GitHub Pages source) and `docs/` are kept in sync by `site/sync_site.js` (copies by filename, never deletes). They have diverged in git. Recommend picking one canonical dir and treating the other as generated; when renaming/adding assets, always mirror both or re-run `sync_site.js`.
- Next session: **Stream B** (emoji audit, Progression panel, parity audit) or the Nikki polish backlog in FX_UI_LAYERING_PLAN.md.

## Session close-out (2026-07-23, earlier)
- **Committed:** `7d2127b` — the 12 verified files (companion, PeaWheel, keybinds, lag, Rojo project fix, docs). Rojo build passes.
- **Scaffolding cleanup done:** `.agents/` untracked + gitignored (kept on disk); junk untracked; superseded phase docs moved to `docs/archive/`; the 15 mojibake gifs renamed to `zundamon_emote_<group><variant>.gif` in both `site/assets/` and `docs/assets/`.

## How to work here (read first)
- **Verify everything in the live game**, not the status docs — historically the docs claimed fixes the code never made.
- Studio is usually open, synced via **chrxxs MCP** + **Rojo on 34872**. Use play mode + `execute_luau` + console output to reproduce and confirm each fix.
- **Source of truth for UI actions is `UIActionRegistry`** (single keyboard dispatcher) + the **Pea Wheel**. Panels expose behaviour via `ActionRegistry.registerCallback(id, fn)`. Do NOT add stray `InputBegan` listeners for panel hotkeys.
- `default.project.json` is Rojo-strict: instance properties go under `$properties`, never as bare keys. Don't add a BOM.

## ✅ Done & verified live
- Companion = real level mesh `Workspace.Meshes/zundapalupdate4` (id 124750913039753), human-scaled, cube impossible.
- PeaWheel module load crash fixed (`GuiService.ReducedMotionEnabled`); builds 8 slices.
- Lag: removed 3 per-frame offenders (SkyOverlay invalid TileOffset, WireframeOutline & ReverbHandler full-workspace scans + bad enum).
- Keybinds: central dispatcher in `UIActionRegistry`; Cook slice wired to K; F1 freed for Keybinds panel.
- Legacy overlay cleanup crash fixed → ~30 legacy scripts + duplicate shells now removed.
- Rojo project parse error fixed (`Lighting.$properties`).

## 🔴 Remaining work — parallelizable streams

### ~~Stream A — Load-time script errors~~ — ✅ DONE (`41ca95e`, verified live: zero client errors)
All 8 items fixed: NPCPatrolSystem Script→ModuleScript, GuestManager Kenney-rig part names (real mesh guests), VN speaker tables exported, CookingService.CookCompleted signal added, AudioEmitter parented to positioned part, 7 FX modules got final returns, DailyController passes Player, ConfigurationFiles path corrected in 5 files (+FontFace fix), stray Workspace plugin Model deleted (needs a place publish to persist).

### Stream B — UI source-of-truth completion — HIGH
1. **Emoji audit** — replace unsupported emoji (notably `🫛` U+1FAD8, and any tofu-box glyphs) with supported emoji or image assets across all UI. This is the "empty boxes."
2. **PeaWheel hub button** — confirm click toggles in play; the tutorial overlay likely intercepts clicks on spawn. Ensure hub button sits above modal overlays or the tutorial yields input.
3. **Progression panel** — `UpdateScript` skips it ("No GUI with MainFrame found"); a deprecated function not yet recovered in the new UI. Rebuild or wire it as a Pea Wheel/HUD action.
4. **Deprecated-function parity audit** — confirm every old-UI function has a new-UI equivalent: inventory, cook, quests, compendium, materials, map, shop, settings, companions, daily, keybinds, progression. (progression is the known gap.)

### Stream C — Companion polish — MEDIUM
1. Orientation constant `ORIENT_CORRECTION` in `CompanionManager.server.lua` (~L340) — user tuning; try `CFrame.Angles(math.rad(180),0,0)` if still inverted.
2. Companion mesh is **untextured (white)** — needs a texture/SurfaceAppearance.
3. Baked animations — needs a rigged/skinned zundapal asset or animation IDs (blocked on asset).

### Stream D — Repo hygiene & commit — MEDIUM
- ~6k lines uncommitted. Commit in logical chunks (fixes / hygiene / formatting).
- Untrack build artifacts + `test_terminal_sim.js`, `mcp_req.json`; gitignore `*.rbxl`, `build/`.
- Reconcile/retire the stale `.agents/` boards (they contradict each other and mislead).
- Validate the wally dependency fork swap (ProfileService/ReplicaService).

## Phase 3 publishing criteria (definition of done)
- [x] Zero load-time script errors in a fresh play session (Stream A — verified live 2026-07-23; needs place publish to persist the Workspace plugin deletion).
- [ ] No tofu/empty-box glyphs anywhere; all UI icons render (Stream B1).
- [ ] Every deprecated UI function has a working new-UI equivalent; no duplicate panels (Stream B).
- [ ] PeaWheel + all 8 slices + all hotkeys single-toggle cleanly; Cook opens (verify live).
- [ ] Companion: correct mesh, upright, human-sized, textured (Stream C1–2).
- [x] Guests render as characters, not capsules (verified live: "Using mesh guest" in console).
- [ ] Multi-player + production DataStore rejoin pass (from PHASE3_ACCEPTANCE_STATUS.md runtime gates).
- [ ] Clean tree committed; Rojo build + focused Selene/StyLua pass (Stream D).
